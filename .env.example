# ==============================================
# Site Configuration
# ==============================================
NEXT_PUBLIC_SITE_URL=https://pastecn.com

# ==============================================
# Vercel Blob Storage (REQUIRED)
# ==============================================
# Automatically generated by Vercel when you enable Blob Storage
# Go to: Vercel Dashboard → Storage → Blob → Enable
# Documentation: https://vercel.com/docs/storage/vercel-blob
BLOB_READ_WRITE_TOKEN=

# ==============================================
# Password Protection Configuration
# ==============================================

# JWT secret for unlock sessions (REQUIRED in production)
# Generate a strong random string:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#   or: openssl rand -hex 32
# WARNING: Use a different secret in production than the default
UNLOCK_SESSION_SECRET=change-this-in-production

# Session duration in hours (default: 24)
# How long users stay authenticated after unlocking a protected snippet
# Recommended values:
#   - 1 hour: High security (sensitive data)
#   - 24 hours: Balanced (default)
#   - 168 hours (7 days): Internal teams
UNLOCK_SESSION_DURATION_HOURS=24

# ==============================================
# Announcement Banner Configuration
# ==============================================

# Set to "true" to enable the announcement banner
NEXT_PUBLIC_ANNOUNCEMENT_ENABLED=false

# The announcement text to display (one-line announcement)
NEXT_PUBLIC_ANNOUNCEMENT_TEXT=

# Optional: Link URL (e.g., GitHub repo for stars)
NEXT_PUBLIC_ANNOUNCEMENT_LINK_URL=

# Optional: Link text (e.g., "Star us on GitHub")
NEXT_PUBLIC_ANNOUNCEMENT_LINK_TEXT=

# Unique ID for this announcement (used for localStorage dismiss tracking)
# Change this when you want to show the announcement again after it was dismissed
NEXT_PUBLIC_ANNOUNCEMENT_ID=

# ==============================================
# Additional Vercel Configuration (Optional)
# ==============================================

# Vercel OIDC Token (automatically set by Vercel CLI)
# Only needed for local Vercel CLI operations
# VERCEL_OIDC_TOKEN=

# ==============================================
# Production Checklist
# ==============================================
# Before deploying to production, ensure:
#   ✅ BLOB_READ_WRITE_TOKEN is set (auto-configured by Vercel)
#   ✅ UNLOCK_SESSION_SECRET is changed to a strong random value
#   ✅ NEXT_PUBLIC_SITE_URL is set to your production domain
#   ✅ Vercel Firewall is enabled with rate limiting rules
#   ✅ (Optional) Vercel Bot Protection is enabled
#
# See VERCEL.md for complete deployment guide
